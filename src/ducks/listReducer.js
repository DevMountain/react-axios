const initialState = {
  loading: false,
  customerList: []
}

// Action Types


// Reducer
export default function listReducer( state = initialState, action ) {
  switch( action.type ) {
    // Pending

    // Fulfilled

    default: return state;
  }
}

// Action Creators
